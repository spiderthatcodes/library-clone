import { FC } from 'react';
import { NavContainer } from './style'

const Navbar: FC = () => {
  return (
    <NavContainer>
        {/* layer one */}
        {/* layer two */}
        {/* layer three, not visible on home page */}
    </NavContainer>
  )
}

export default Navbar
